import React from 'react'

const inner = () => {
  return (
    <div>inner text</div>
  )
}

export default inner

// when ignoring a directoy in routing, we don't have to create a page.js file in the ignored directory